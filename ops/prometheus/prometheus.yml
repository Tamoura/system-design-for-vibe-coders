# Lesson 7.2: Prometheus scrapes the collector and evaluates the alert rules.
global:
  scrape_interval: 15s
  evaluation_interval: 30s
rule_files: [alerts.yml]
scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
