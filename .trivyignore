# Lesson 8.1: accepted vulnerabilities. One CVE id per line, each with WHY it does not apply
# to Beacon and WHEN to look again, e.g.:
#   CVE-2099-12345  # only exploitable with feature X, which Beacon does not use; review 2027-01-01
